import appLogo from '@/assets/logo.png';
import { ThemeConfig } from 'antd';

export const APP_LOGO = appLogo;

export const APP_THEME: ThemeConfig = { token: {} };

export const DAYJS_FORMAT = 'MMM D, YYYY';
